#!/usr/bin/env node

var async = require('async'),
    shell = require('shelljs');

module.exports = function (exit) {
    async.series([
        require('./test-lint.js'),
        require('./test-unit.js'),
        require('./test-system.js')
    ], exit);
};

!module.parent && module.exports(shell.exit);
